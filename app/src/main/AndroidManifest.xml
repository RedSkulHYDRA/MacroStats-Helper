<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Usage stats permission (special permission) -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MacroStatsHelper"
        tools:targetApi="31">

        <activity
            android:name=".core.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MacroStatsHelper.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Settings Activity -->
        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false"
            android:label="Display Settings"
            android:theme="@style/Theme.MacroStatsHelper.NoActionBar" />

        <!-- QS Tile Settings Activity -->
        <activity
            android:name=".tiles.QSTileSettingsActivity"
            android:exported="false"
            android:label="QS Tile Settings"
            android:theme="@style/Theme.MacroStatsHelper.NoActionBar" />

        <!-- Foreground service for continuous data monitoring -->
        <service
            android:name=".data.DataUsageService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Background service for battery monitoring -->
        <service
            android:name=".data.BatteryService"
            android:enabled="true"
            android:exported="false" />

        <!-- Boot receiver for auto-start -->
        <receiver
            android:name=".core.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver for Update Stats button -->
        <receiver
            android:name=".core.UpdateStatsReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.redskul.macrostatshelper.UPDATE_STATS" />
            </intent-filter>
        </receiver>

        <!-- QS Tile Services -->
        <service
            android:name=".tiles.WiFiDataUsageQSTileService"
            android:exported="true"
            android:icon="@drawable/ic_wifi"
            android:label="@string/wifi_usage"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".tiles.MobileDataUsageQSTileService"
            android:exported="true"
            android:icon="@drawable/ic_mobile"
            android:label="@string/mobile_data"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".tiles.ChargeQSTileService"
            android:exported="true"
            android:icon="@drawable/ic_battery_cc"
            android:label="@string/charge_cycles"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".tiles.BatteryHealthQSTileService"
            android:exported="true"
            android:icon="@drawable/ic__battery_health"
            android:label="@string/battery_health"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".tiles.ScreenTimeoutQSTileService"
            android:exported="true"
            android:icon="@drawable/ic_screen_timeout"
            android:label="@string/screen_timeout"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

    </application>
</manifest>
